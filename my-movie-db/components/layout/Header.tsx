import SearchBar from "../common/SearchBar";

const Header = () => {
  return (
      <header className="flex justify-center mb-6 py-9">
        <SearchBar />
      </header>
  );
};

export default Header;
